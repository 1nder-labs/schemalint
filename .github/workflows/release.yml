# Hand-maintained release workflow, originally bootstrapped by cargo-dist.
# Do not overwrite it with `dist init`: this file owns action pins, artifact
# identity, and channel gates while invoking `dist plan/build` as tools.
#
# Copyright 2022-2024, axodotdev
# SPDX-License-Identifier: MIT or Apache-2.0
#
# CI that:
#
# * checks for a Git Tag that looks like a release
# * builds artifacts with dist (archives, installers, hashes)
# * uploads those artifacts to temporary workflow zip
# * on success, uploads the artifacts to a GitHub Release
#
# Note that the GitHub Release will be created with a generated
# title/body based on your changelogs.
#
# --- Adapted with full release pipeline ---
# Adds: build-wheel, smoke-test (ubuntu/macos/windows), publish-crates,
# publish-pypi, create-github-release, smoke-npm, publish-npm.
# The cargo-dist host job is superseded by create-github-release which
# creates immutable assets after local smokes and before the npm wrapper's
# real auto-download smoke; registry publishes remain gated behind that smoke.

name: Release
permissions:
  "contents": "read"
env:
  # A tag rerun must use the same compiler rather than resolving today's
  # mutable `stable` alias and producing different release bytes.
  RUSTUP_TOOLCHAIN: '1.93.1'

# This task will run whenever you push a git tag that looks like a version
# like "1.0.0", "v0.1.0-prerelease.1", "my-app/0.1.0", "releases/v1.0.0", etc.
# Various formats will be parsed into a VERSION and an optional PACKAGE_NAME, where
# PACKAGE_NAME must be the name of a Cargo package in your workspace, and VERSION
# must be a Cargo-style SemVer Version (must have at least major.minor.patch).
#
# If PACKAGE_NAME is specified, then the announcement will be for that
# package (erroring out if it doesn't have the given version or isn't dist-able).
#
# If PACKAGE_NAME isn't specified, then the announcement will be for all
# (dist-able) packages in the workspace with that version (this mode is
# intended for workspaces with only one dist-able package, or with all dist-able
# packages versioned/released in lockstep).
#
# If you push multiple tags at once, separate instances of this workflow will
# spin up, creating an independent announcement for each one. However, GitHub
# will hard limit this to 3 tags per commit, as it will assume more tags is a
# mistake.
#
# If there's a prerelease-style suffix to the version, then the release(s)
# will be marked as a prerelease.
on:
  # Release-only: no pull_request trigger. Every build/publish job here is tag-gated, so on a
  # PR they'd only surface as skipped-check noise. Workflow YAML is validated on PRs by the
  # `actionlint` job in ci.yml instead. (The plan job's `!github.event.pull_request` guards are
  # inherited from cargo-dist and harmlessly resolve to the tag path.)
  push:
    tags:
      - '**[0-9]+.[0-9]+.[0-9]+*'
  # release-please dispatches this workflow at the freshly-created tag. workflow_dispatch
  # fires even from GITHUB_TOKEN (unlike a tag push), so no PAT is needed to chain the two.
  workflow_dispatch:

jobs:
  # ==========================================================================
  # cargo-dist execution jobs (hand-maintained orchestration)
  # ==========================================================================

  # Run 'dist plan' (or host) to determine what tasks we need to do
  plan:
    runs-on: "ubuntu-22.04"
    timeout-minutes: 15
    outputs:
      val: ${{ steps.plan.outputs.manifest }}
      tag: ${{ !github.event.pull_request && github.ref_name || '' }}
      tag-flag: ${{ !github.event.pull_request && format('--tag={0}', github.ref_name) || '' }}
      publishing: ${{ !github.event.pull_request }}
    env:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
        with:
          persist-credentials: false
          submodules: recursive
      - name: Install dist
        # Exact crate version plus --locked uses crates.io's immutable package
        # checksum and the release's committed dependency lock.
        run: cargo install cargo-dist --version 0.31.0 --locked
      - name: Cache dist
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6
        with:
          name: cargo-dist-cache
          path: ~/.cargo/bin/dist
      # sure would be cool if github gave us proper conditionals...
      # so here's a doubly-nested ternary-via-truthiness to try to provide the best possible
      # functionality based on whether this is a pull_request, and whether it's from a fork.
      # (PRs run on the *source* but secrets are usually on the *target* -- that's *good*
      # but also really annoying to build CI around when it needs secrets to work right.)
      - id: plan
        run: |
          dist ${{ (!github.event.pull_request && format('host --steps=create --tag={0}', github.ref_name)) || 'plan' }} --output-format=json > plan-dist-manifest.json
          echo "dist ran successfully"
          cat plan-dist-manifest.json
          echo "manifest=$(jq -c "." plan-dist-manifest.json)" >> "$GITHUB_OUTPUT"
      - name: "Upload dist-manifest.json"
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6
        with:
          name: artifacts-plan-dist-manifest
          path: plan-dist-manifest.json

  # Build and packages all the platform-specific things
  build-local-artifacts:
    name: build-local-artifacts (${{ join(matrix.targets, ', ') }})
    # Let the initial task tell us to not run (currently very blunt)
    needs:
      - plan
    if: ${{ fromJson(needs.plan.outputs.val).ci.github.artifacts_matrix.include != null && (needs.plan.outputs.publishing == 'true' || fromJson(needs.plan.outputs.val).ci.github.pr_run_mode == 'upload') }}
    strategy:
      fail-fast: false
      # Target platforms/runners are computed by dist in create-release.
      # Each member of the matrix has the following arguments:
      #
      # - runner: the github runner
      # - dist-args: cli flags to pass to dist
      # - install-dist: expression to run to install dist on the runner
      #
      # Typically there will be:
      # - 1 "global" task that builds universal installers
      # - N "local" tasks that build each platform's binaries and platform-specific installers
      matrix: ${{ fromJson(needs.plan.outputs.val).ci.github.artifacts_matrix }}
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 30
    container: ${{ matrix.container && matrix.container.image || null }}
    env:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      BUILD_MANIFEST_NAME: target/distrib/${{ join(matrix.targets, '-') }}-dist-manifest.json
    steps:
      - name: enable windows longpaths
        run: |
          git config --global core.longpaths true
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
        with:
          persist-credentials: false
          submodules: recursive
      - name: Install pinned release Rust toolchain
        uses: dtolnay/rust-toolchain@4be7066ada62dd38de10e7b70166bc74ed198c30 # stable action
        with:
          toolchain: '1.93.1'
      - name: Install cached dist
        uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131 # v7
        with:
          name: cargo-dist-cache
          path: ~/.cargo/bin/
      - run: chmod +x ~/.cargo/bin/dist
      # Get the dist-manifest
      - name: Fetch local artifacts
        uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131 # v7
        with:
          pattern: artifacts-*
          path: target/distrib/
          merge-multiple: true
      - name: Install dependencies
        run: |
          ${{ matrix.packages_install }}
      - name: Build artifacts
        run: |
          # Actually do builds and make zips and whatnot
          dist build ${{ needs.plan.outputs.tag-flag }} --print=linkage --output-format=json ${{ matrix.dist_args }} > dist-manifest.json
          echo "dist ran successfully"
      - id: cargo-dist
        name: Post-build
        # We force bash here just because github makes it really hard to get values up
        # to "real" actions without writing to env-vars, and writing to env-vars has
        # inconsistent syntax between shell and powershell.
        shell: bash
        run: |
          # Parse out what we just built and upload it to scratch storage
          echo "paths<<EOF" >> "$GITHUB_OUTPUT"
          dist print-upload-files-from-manifest --manifest dist-manifest.json >> "$GITHUB_OUTPUT"
          echo "EOF" >> "$GITHUB_OUTPUT"

          cp dist-manifest.json "$BUILD_MANIFEST_NAME"
      - name: "Upload artifacts"
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6
        with:
          name: artifacts-build-local-${{ join(matrix.targets, '_') }}
          path: |
            ${{ steps.cargo-dist.outputs.paths }}
            ${{ env.BUILD_MANIFEST_NAME }}

  # Build and package all the platform-agnostic(ish) things
  build-global-artifacts:
    needs:
      - plan
      - build-local-artifacts
    runs-on: "ubuntu-22.04"
    timeout-minutes: 15
    env:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      BUILD_MANIFEST_NAME: target/distrib/global-dist-manifest.json
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
        with:
          persist-credentials: false
          submodules: recursive
      - name: Install cached dist
        uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131 # v7
        with:
          name: cargo-dist-cache
          path: ~/.cargo/bin/
      - run: chmod +x ~/.cargo/bin/dist
      # Get all the local artifacts for the global tasks to use (for e.g. checksums)
      - name: Fetch local artifacts
        uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131 # v7
        with:
          pattern: artifacts-*
          path: target/distrib/
          merge-multiple: true
      - id: cargo-dist
        shell: bash
        run: |
          dist build ${{ needs.plan.outputs.tag-flag }} --output-format=json "--artifacts=global" > dist-manifest.json
          echo "dist ran successfully"

          # Parse out what we just built and upload it to scratch storage
          echo "paths<<EOF" >> "$GITHUB_OUTPUT"
          jq --raw-output ".upload_files[]" dist-manifest.json >> "$GITHUB_OUTPUT"
          echo "EOF" >> "$GITHUB_OUTPUT"

          cp dist-manifest.json "$BUILD_MANIFEST_NAME"
      - name: "Upload artifacts"
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6
        with:
          name: artifacts-build-global
          path: |
            ${{ steps.cargo-dist.outputs.paths }}
            ${{ env.BUILD_MANIFEST_NAME }}

  # ==========================================================================
  # Custom release pipeline jobs
  #
  # Dependency chain:
  #   [dist builds + wheel] --> smoke-wheel --> prepare-release
  #       (manifest + one npm pack) --> smoke-test --> create-github-release --> smoke-npm
  #       --> publish-crates + publish-pypi + publish-npm --> registry-readback
  #
  # All custom jobs gate on startsWith(github.ref, 'refs/tags/v') so they
  # only execute on tag pushes, not on PR plan-only runs.
  #
  # Release SOP — tag naming:
  #   Tags MUST be pushed as vMAJOR.MINOR.PATCH (e.g. v1.0.0). A bare
  #   1.0.0 tag satisfies the cargo-dist on.push.tags pattern above but
  #   is NOT a supported release trigger for the custom jobs: all of them
  #   gate on startsWith(github.ref, 'refs/tags/v'), so a tag without the
  #   leading "v" will run the dist build but skip every custom job
  #   (wheel, smoke, publish, release creation). Always use the v prefix.
  #
  # Artifact-action versions:
  #   The dist execution jobs and custom jobs pin the same artifact-action
  #   generations (upload-artifact@v6 / download-artifact@v7)
  #   (upload-artifact@v6 / download-artifact@v7) for artifact-transfer
  #   compatibility — cross-major transfers (e.g. v4 download of a v6 upload)
  #   are fragile per GitHub docs. Update these pins deliberately and together.
  # ==========================================================================

  # Build one platform wheel for every declared native release target.
  build-wheel:
    needs:
      - build-local-artifacts
      - build-global-artifacts
    if: ${{ startsWith(github.ref, 'refs/tags/v') }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: ubuntu-24.04
            target: x86_64-unknown-linux-gnu
            manylinux: '2_28'
          - runner: ubuntu-24.04-arm
            target: aarch64-unknown-linux-gnu
            manylinux: '2_28'
          - runner: macos-15-intel
            target: x86_64-apple-darwin
            manylinux: '2_28'
          - runner: macos-15
            target: aarch64-apple-darwin
            manylinux: '2_28'
          - runner: windows-2025
            target: x86_64-pc-windows-msvc
            manylinux: '2_28'
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          persist-credentials: false

      - name: Build wheel
        uses: PyO3/maturin-action@86b9d133d34bc1b40018696f782949dac11bd380 # v1.49.4
        with:
          command: build
          target: ${{ matrix.target }}
          manylinux: ${{ matrix.manylinux }}
          maturin-version: v1.13.1
          rust-toolchain: '1.93.1'
          working-directory: crates/schemalint-python
          args: --release --locked --compatibility pypi --out ../../dist

      - name: Upload wheel artifact
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6
        with:
          name: wheel-${{ matrix.target }}
          path: dist/*.whl

  # Install the exact platform wheel and run the real Pydantic fixture on the
  # matching architecture before the wheels can enter the release bundle.
  smoke-wheel:
    needs:
      - build-wheel
    if: ${{ startsWith(github.ref, 'refs/tags/v') }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: ubuntu-24.04
            target: x86_64-unknown-linux-gnu
          - runner: ubuntu-24.04-arm
            target: aarch64-unknown-linux-gnu
          - runner: macos-15-intel
            target: x86_64-apple-darwin
          - runner: macos-15
            target: aarch64-apple-darwin
          - runner: windows-2025
            target: x86_64-pc-windows-msvc
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          persist-credentials: false

      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: '3.12'

      - name: Download matching wheel
        uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131 # v7
        with:
          name: wheel-${{ matrix.target }}
          path: wheels

      - name: Exercise installed wheel against Pydantic 1 and 2
        shell: bash
        run: |
          set -euo pipefail
          for SPEC in 'pydantic>=1.10,<2' 'pydantic>=2,<3'; do
            python -m pip install --force-reinstall "$SPEC"
            python -m pip install --force-reinstall --no-deps wheels/*.whl
            python scripts/smoke-installed-wheel.py
            python -m pip uninstall -y schemalint pydantic
          done

  # Assemble final bytes, generate the npm-shipped digest manifest, and pack
  # npm exactly once. Every later job consumes this immutable bundle.
  prepare-release:
    needs:
      - build-local-artifacts
      - build-global-artifacts
      - build-wheel
      - smoke-wheel
    if: ${{ startsWith(github.ref, 'refs/tags/v') }}
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          persist-credentials: false

      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: '22'

      - name: Install pinned release Rust toolchain
        uses: dtolnay/rust-toolchain@4be7066ada62dd38de10e7b70166bc74ed198c30 # stable action
        with:
          toolchain: '1.93.1'

      - name: Download final native artifacts
        uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131 # v7
        with:
          pattern: artifacts-build-*
          path: release-bundle
          merge-multiple: true

      - name: Download final wheels
        uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131 # v7
        with:
          pattern: wheel-*
          path: release-bundle
          merge-multiple: true

      - name: Remove internal dist manifests
        run: rm -f release-bundle/*-dist-manifest.json

      - name: Validate tag and package version parity
        run: node scripts/validate-native-manifest.cjs --versions-only --tag "${{ github.ref_name }}"
        working-directory: npm/schemalint

      - name: Stage and reproduce the crates.io package bytes
        run: |
          set -euo pipefail
          cargo package --locked -p schemalint
          cp target/package/schemalint-*.crate release-bundle/
          cargo package --locked -p schemalint
          cmp target/package/schemalint-*.crate release-bundle/schemalint-*.crate

      - name: Generate immutable native manifest from final archives
        run: node scripts/generate-native-manifest.cjs ../../release-bundle native-manifest.json
        working-directory: npm/schemalint

      - name: Build and pack npm once
        run: |
          npm ci
          npm run build
          node scripts/validate-native-manifest.cjs --release --tag "${{ github.ref_name }}"
          npm pack --pack-destination ../../release-bundle
        working-directory: npm/schemalint

      - name: Upload immutable release bundle
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6
        with:
          name: release-bundle
          path: release-bundle/*

  # Smoke-test binaries on all three platforms
  smoke-test:
    needs:
      - prepare-release
    if: ${{ startsWith(github.ref, 'refs/tags/v') }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            ext: tar.gz
          - os: macos-latest
            target: aarch64-apple-darwin
            ext: tar.gz
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            ext: zip
    runs-on: ${{ matrix.os }}
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          persist-credentials: false

      - name: Download immutable release bundle
        uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131 # v7
        with:
          name: release-bundle
          path: release-bundle

      - name: Extract and smoke-test binary (Unix)
        if: ${{ matrix.os != 'windows-latest' }}
        shell: bash
        run: |
          set -e
          ARCHIVE="schemalint-${{ matrix.target }}.${{ matrix.ext }}"
          echo "Extracting: $ARCHIVE"
          mkdir -p _smoke
          tar xzf "release-bundle/$ARCHIVE" -C _smoke
          BIN="$(find _smoke -maxdepth 3 -type f -name schemalint | head -n1)"
          if [ -z "$BIN" ]; then
            echo "::error::schemalint binary not found in archive $ARCHIVE"
            ls -R _smoke
            exit 1
          fi
          chmod +x "$BIN"
          echo "Running: schemalint --version"
          VERSION_OUTPUT=$("$BIN" --version)
          echo "$VERSION_OUTPUT"
          echo "Running: schemalint check (minimal schema)"
          echo '{"type": "object", "properties": {"name": {"type": "string"}}, "required": ["name"], "additionalProperties": false}' > test-schema.json
          "$BIN" check test-schema.json --profile openai.so.2026-04-30

      - name: Extract and smoke-test binary (Windows)
        if: ${{ matrix.os == 'windows-latest' }}
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          $archive = "schemalint-${{ matrix.target }}.${{ matrix.ext }}"
          Write-Host "Extracting: $archive"
          New-Item -ItemType Directory -Force -Path _smoke | Out-Null
          Expand-Archive -Path "release-bundle/$archive" -DestinationPath _smoke
          $bin = Get-ChildItem -Path _smoke -Recurse -Filter schemalint.exe | Select-Object -First 1
          if (-not $bin) {
            Write-Error "schemalint.exe not found in archive $archive"
            Get-ChildItem -Path _smoke -Recurse
            exit 1
          }
          # Do NOT capture native stdout into a variable: under $ErrorActionPreference=Stop,
          # pwsh 7.4 throws "StandardOutputEncoding is only supported when standard output is
          # redirected". Run directly and check $LASTEXITCODE explicitly.
          Write-Host "Running: schemalint --version"
          & $bin.FullName --version
          if ($LASTEXITCODE -ne 0) { throw "schemalint --version failed (exit $LASTEXITCODE)" }
          Write-Host "Running: schemalint check (minimal schema)"
          '{"type": "object", "properties": {"name": {"type": "string"}}, "required": ["name"], "additionalProperties": false}' | Out-File -Encoding ascii test-schema.json
          & $bin.FullName check test-schema.json --profile openai.so.2026-04-30
          if ($LASTEXITCODE -ne 0) { throw "schemalint check failed (exit $LASTEXITCODE)" }

      - name: Verify staged npm tarball inventory (ubuntu only)
        if: ${{ matrix.os == 'ubuntu-latest' }}
        shell: bash
        run: |
          mkdir npm-smoke
          tar xzf release-bundle/*.tgz -C npm-smoke
          test -f npm-smoke/package/native-manifest.json
          test -f npm-smoke/package/launcher/integrity.cjs

  # Publish Rust crates to crates.io
  publish-crates:
    needs:
      - smoke-npm
    if: ${{ startsWith(github.ref, 'refs/tags/v') }}
    runs-on: ubuntu-latest
    # Single schemalint crate (profiles folded in); one verify-build, allow headroom.
    timeout-minutes: 15
    permissions:
      id-token: write
      contents: read
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          persist-credentials: false

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@4be7066ada62dd38de10e7b70166bc74ed198c30 # stable action
        with:
          toolchain: '1.93.1'

      - name: Cache cargo dependencies
        uses: Swatinem/rust-cache@42dc69e1aa15d09112580998cf2ef0119e2e91ae # v2

      - name: Download staged crates.io package
        uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131 # v7
        with:
          name: release-bundle
          path: release-bundle

      - name: Verify any existing crates.io artifact before reuse
        id: existing_crate
        shell: bash
        run: |
          set -euo pipefail
          VERSION="${GITHUB_REF_NAME#v}"
          if ! curl --fail --silent \
            "https://crates.io/api/v1/crates/schemalint/$VERSION" -o crate.json; then
            echo "exists=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          EXPECTED=$(sha256sum release-bundle/schemalint-*.crate | cut -d' ' -f1)
          ACTUAL=$(jq -r '.version.checksum' crate.json)
          test "$ACTUAL" = "$EXPECTED"
          echo "exists=true" >> "$GITHUB_OUTPUT"

      # Cargo's public publish command does not accept a prebuilt .crate path;
      # pin Cargo, prove its deterministic repack matches the staged bytes, and
      # enforce exact registry digest readback after publication.
      - name: Prove pinned Cargo recreates the staged package
        if: ${{ steps.existing_crate.outputs.exists != 'true' }}
        run: |
          cargo package --locked -p schemalint
          cmp target/package/schemalint-*.crate release-bundle/schemalint-*.crate

      - name: Authenticate to crates.io via OIDC (trusted publishing)
        if: ${{ steps.existing_crate.outputs.exists != 'true' }}
        uses: rust-lang/crates-io-auth-action@c6f97d42243bad5fab37ca0427f495c86d5b1a18 # v1.0.5
        id: auth

      - name: Publish schemalint
        if: ${{ steps.existing_crate.outputs.exists != 'true' }}
        run: |
          set -euo pipefail
          published=false
          for i in 1 2 3; do
            out=$(cargo publish --locked -p schemalint 2>&1) && { published=true; echo "$out"; break; }
            echo "$out"
            if echo "$out" | grep -qiE "already (uploaded|exists)"; then
              VERSION="${GITHUB_REF_NAME#v}"
              EXPECTED=$(sha256sum release-bundle/schemalint-*.crate | cut -d' ' -f1)
              ACTUAL=$(curl --fail --silent \
                "https://crates.io/api/v1/crates/schemalint/$VERSION" | jq -r '.version.checksum')
              test "$ACTUAL" = "$EXPECTED"
              echo "crate version already published with identical bytes"
              published=true
              break
            fi
            echo "cargo publish attempt $i failed, retrying in 10s..."
            sleep 10
          done
          if [ "$published" != true ]; then
            echo "::error::cargo publish failed after 3 attempts"
            exit 1
          fi
        env:
          CARGO_REGISTRY_TOKEN: ${{ steps.auth.outputs.token }}

  # Publish Python wheel to PyPI
  publish-pypi:
    needs:
      - prepare-release
      - smoke-npm
    # PyPI is an explicit channel. Enable it only after configuring this
    # environment as a trusted publisher and setting PUBLISH_PYPI=true.
    if: ${{ startsWith(github.ref, 'refs/tags/v') && vars.PUBLISH_PYPI == 'true' }}
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      id-token: write
      contents: read
    environment:
      name: pypi
      url: https://pypi.org/project/schemalint/
    steps:
      - name: Download wheel artifact
        uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131 # v7
        with:
          name: release-bundle
          path: release-bundle

      - name: Isolate staged wheel
        run: |
          mkdir pypi-dist
          cp release-bundle/*.whl pypi-dist/

      - name: Verify any existing PyPI artifact before reuse
        id: existing_pypi
        shell: bash
        run: |
          set -euo pipefail
          VERSION="${GITHUB_REF_NAME#v}"
          if ! curl --fail --silent "https://pypi.org/pypi/schemalint/$VERSION/json" -o pypi.json; then
            echo "exists=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          test "$(find pypi-dist -maxdepth 1 -name '*.whl' | wc -l | tr -d ' ')" = \
            "$(jq '.urls | length' pypi.json)"
          for WHEEL in pypi-dist/*.whl; do
            FILENAME=$(basename "$WHEEL")
            EXPECTED=$(sha256sum "$WHEEL" | cut -d' ' -f1)
            ACTUAL=$(jq -r --arg filename "$FILENAME" \
              '.urls[] | select(.filename == $filename) | .digests.sha256' pypi.json)
            test -n "$ACTUAL"
            test "$ACTUAL" = "$EXPECTED"
          done
          echo "exists=true" >> "$GITHUB_OUTPUT"

      - name: Publish to PyPI
        if: ${{ steps.existing_pypi.outputs.exists != 'true' }}
        uses: pypa/gh-action-pypi-publish@cef221092ed1bacb1cc03d23a2d87d1d172e277b # release/v1
        with:
          packages-dir: pypi-dist/
          skip-existing: true

  # Create GitHub assets only after every local artifact smoke passes.
  create-github-release:
    needs:
      - smoke-test
      - prepare-release
    if: ${{ needs.smoke-test.result == 'success' && startsWith(github.ref, 'refs/tags/v') }}
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          persist-credentials: false
          fetch-depth: 0

      - name: Download immutable release bundle
        uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131 # v7
        with:
          name: release-bundle
          path: release-artifacts

      - name: Refuse to replace an existing release bundle
        id: existing_release
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          if ! gh release view "$GITHUB_REF_NAME" >/dev/null 2>&1; then
            echo "exists=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          mkdir existing-release
          gh release download "$GITHUB_REF_NAME" --dir existing-release
          test "$(find release-artifacts -maxdepth 1 -type f | wc -l)" = \
            "$(find existing-release -maxdepth 1 -type f | wc -l)"
          for LOCAL in release-artifacts/*; do
            REMOTE="existing-release/$(basename "$LOCAL")"
            test -f "$REMOTE"
            cmp "$LOCAL" "$REMOTE"
          done
          echo "exists=true" >> "$GITHUB_OUTPUT"

      - name: Create GitHub Release
        if: ${{ steps.existing_release.outputs.exists != 'true' }}
        uses: softprops/action-gh-release@3bb12739c298aeb8a4eeaf626c5b8d85266b0e65 # v2
        with:
          tag_name: ${{ github.ref_name }}
          files: |
            release-artifacts/*
          generate_release_notes: true
          fail_on_unmatched_files: true
          overwrite_files: false

  # Smoke-test npm wrapper auto-download (requires GitHub Release to exist)
  smoke-npm:
    needs:
      - create-github-release
      - prepare-release
    # Install and exercise the exact tarball packed before GitHub assets existed.
    if: ${{ !cancelled() && needs.create-github-release.result == 'success' && startsWith(github.ref, 'refs/tags/v') }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 10
    env:
      SCHEMALINT_CACHE_DIR: ${{ github.workspace }}/schemalint-cache
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          persist-credentials: false

      - name: Setup Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: '22'

      - name: Download the exact staged npm tarball
        uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131 # v7
        with:
          name: release-bundle
          path: release-bundle

      - name: Install packed package globally
        shell: bash
        run: npm install -g release-bundle/*.tgz

      - name: Smoke-test installed CLI
        shell: bash
        run: |
          schemalint --version
          echo '{"type": "object", "properties": {"name": {"type": "string"}}, "required": ["name"], "additionalProperties": false}' > test-schema.json
          schemalint check test-schema.json --profile openai.so.2026-04-30

      - name: Smoke packed structured-output SDK consumers (ubuntu only)
        if: ${{ matrix.os == 'ubuntu-latest' }}
        shell: bash
        run: |
          node scripts/smoke-packed-sdk-consumers.cjs \
            release-bundle/*.tgz current "$(command -v schemalint)"

  # Publish all npm packages
  publish-npm:
    needs:
      - smoke-npm
      - prepare-release
    # @1nder-labs/schemalint was bootstrapped by hand for its first publish; from here on
    # publish-npm runs on every tagged release via OIDC trusted publishing (no token, no OTP).
    if: ${{ !cancelled() && needs.smoke-npm.result == 'success' && startsWith(github.ref, 'refs/tags/v') }}
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      id-token: write
      contents: read
    environment:
      name: npm
      url: https://www.npmjs.com/package/@1nder-labs/schemalint
    steps:
      - name: Setup Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: '22'
          registry-url: 'https://registry.npmjs.org'

      - name: Install pinned npm for trusted publishing
        run: npm install -g npm@11.6.2

      - name: Download the exact staged npm tarball
        uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131 # v7
        with:
          name: release-bundle
          path: release-bundle

      - name: Publish @1nder-labs/schemalint
        run: |
          published=false
          for i in 1 2 3; do
            out=$(npm publish release-bundle/*.tgz --access public 2>&1) && { published=true; echo "$out"; break; }
            echo "$out"
            if echo "$out" | grep -qiE "previously published|cannot publish over|You cannot publish over|already exists"; then
              echo "package version already published; treating as success"
              published=true; break
            fi
            echo "npm publish attempt $i failed, retrying in 10s..."
            sleep 10
          done
          if [ "$published" != true ]; then
            echo "::error::npm publish failed after 3 attempts"
            exit 1
          fi

  registry-readback:
    needs:
      - publish-crates
      - publish-pypi
      - publish-npm
    if: ${{ always() && needs.publish-crates.result == 'success' && needs.publish-npm.result == 'success' && (needs.publish-pypi.result == 'success' || needs.publish-pypi.result == 'skipped') && startsWith(github.ref, 'refs/tags/v') }}
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: '22'

      - name: Download staged artifacts for digest readback
        uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131 # v7
        with:
          name: release-bundle
          path: release-bundle

      - name: Read back npm registry version and exact tarball digest
        shell: bash
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          test "$(npm view "@1nder-labs/schemalint@$VERSION" version)" = "$VERSION"
          EXPECTED="sha512-$(openssl dgst -sha512 -binary release-bundle/*.tgz | openssl base64 -A)"
          test "$(npm view "@1nder-labs/schemalint@$VERSION" dist.integrity)" = "$EXPECTED"

      - name: Read back crates.io exact package digest
        shell: bash
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          EXPECTED=$(sha256sum release-bundle/schemalint-*.crate | cut -d' ' -f1)
          ACTUAL=$(curl --fail --silent \
            "https://crates.io/api/v1/crates/schemalint/$VERSION" | jq -r '.version.checksum')
          test "$ACTUAL" = "$EXPECTED"

      - name: Read back PyPI exact wheel digest when published
        if: ${{ needs.publish-pypi.result == 'success' }}
        shell: bash
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          curl --fail --silent "https://pypi.org/pypi/schemalint/$VERSION/json" -o pypi.json
          test "$(find release-bundle -maxdepth 1 -name '*.whl' | wc -l | tr -d ' ')" = \
            "$(jq '.urls | length' pypi.json)"
          for WHEEL in release-bundle/*.whl; do
            FILENAME=$(basename "$WHEEL")
            EXPECTED=$(sha256sum "$WHEEL" | cut -d' ' -f1)
            ACTUAL=$(jq -r --arg filename "$FILENAME" \
              '.urls[] | select(.filename == $filename) | .digests.sha256' pypi.json)
            test -n "$ACTUAL"
            test "$ACTUAL" = "$EXPECTED"
          done
